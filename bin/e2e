#!/usr/bin/env bash
# Tests de bout en bout : vrai serveur + Chrome headless (Playwright, Chrome système).
# BELOTE_SHOTS=dossier pour garder des captures d'écran.
set -euo pipefail
cd "$(dirname "$0")/.."
exec .venv/bin/python -m pytest -q -m e2e "$@"
