#!/usr/bin/env bash
# Sert la page en local : http://localhost:8010 (même page que sur GitHub Pages).
# Usage : bin/dev [port]
set -euo pipefail
cd "$(dirname "$0")/.."
exec python3 -m http.server "${1:-8010}" --bind 127.0.0.1 --directory frontend
